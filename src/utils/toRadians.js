// Converts degrees to radians for geospatial calculations.

/** Converts degrees to radians for trigonometry helpers. */
export default function toRadians(value) {
  return (value * Math.PI) / 180;
}
