// Entry point component that mounts the Transit screen used across the app.

import React from 'react';
import TransitScreen from './src/screens/TransitScreen.js';

/** Root component that mounts the Transit screen for Expo to render. */
export default function App() {
  return <TransitScreen />;
}
